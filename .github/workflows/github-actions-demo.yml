name: Build
on: push
jobs:
  build_ebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare starting docker image
        uses: docker://gentoo/stage3-amd64
      - uses: actions/checkout@v2
      - run: |
          ls /usr/portage
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
